spring:
  application:
    name: spring-demo
  datasource:
    url: jdbc:mysql://localhost:3306/demo-db?createDatabaseIfNotExist=true
    username: root
    password: admin
  jpa:
    show-sql: true

payment-gateway: blik

stripe:
  supported-currencies: USD,EUR,PLN
